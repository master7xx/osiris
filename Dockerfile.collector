FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY tsconfig.json ./
COPY src ./src
COPY tools ./tools
COPY migrations ./migrations
USER node
CMD ["sh", "-c", "npm run events:migrate && npm run events:collect"]
