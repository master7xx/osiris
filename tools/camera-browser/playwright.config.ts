import { fileURLToPath } from 'node:url';
import { defineConfig } from '@playwright/test';
export default defineConfig({ testDir: '.', outputDir: fileURLToPath(new URL('../../test-results', import.meta.url)), testMatch: '*.spec.ts', workers: 1, use: { channel: 'chrome', baseURL: 'http://127.0.0.1:4177', viewport: { width: 1280, height: 800 }, screenshot: 'only-on-failure', trace: 'retain-on-failure' }, webServer: { cwd: fileURLToPath(new URL('../../', import.meta.url)), command: 'node tools/camera-browser/server.mjs', url: 'http://127.0.0.1:4177/tools/camera-browser/', timeout: 60000 }, reporter: 'list' });
